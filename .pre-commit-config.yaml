repos:
  - repo: local
    hooks:
      - id: run-secret-searcher
        name: Run Secret Searcher check on staged files
        entry: python3 src/search.py
        language: python
        always_run: true
        pass_filenames: true

